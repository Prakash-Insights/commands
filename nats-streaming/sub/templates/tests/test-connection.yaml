apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sub.fullname" . }}-test-connection"
  labels:
{{ include "sub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
